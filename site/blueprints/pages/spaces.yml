title: Spaces
icon: dashboard
options:
  url: false
  status: false
  delete: false
  
extends: layouts/feeds

fields:
  feeds:
    label: Spaces
    query: site.children.template('projet').filterBy('feedspaces', '*=', 'true')
    text: '{{ page.legendeAuto.toCaption("spaces") }}'
    image:
      query: page.feedspacesimage.toFile

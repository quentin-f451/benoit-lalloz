title: Collections
icon: dashboard
options:
  url: false
  status: false
  delete: false

extends: layouts/feeds

fields:
  feeds:
    label: Collections
    query: site.children.template('projet').filterBy('feedcollections', '*=', 'true')
    text: '{{ page.legendeAuto.toCaption("collections") }}'
    image:
      query: page.feedcollectionsimage.toFile

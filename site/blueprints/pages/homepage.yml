title: Homepage
icon: dashboard
options:
  url: false
  status: false
  delete: false

extends: layouts/feeds

fields:
  feeds:
    label: Homepage
    query: site.children.template('projet').filterBy('feedhome', '*=', 'true')
    size: tiny
    image:
      query: page.feedhomeimage.toFile

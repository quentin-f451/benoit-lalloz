title: Laboratory
icon: dashboard
options:
  url: false
  status: false
  delete: false
  
extends: layouts/feeds

fields:
  feeds:
    label: Laboratory
    query: site.children.template('projet').filterBy('feedlaboratory', '*=', 'true')
    text: '{{ page.legendeAuto.toCaption("laboratory") }}'
    image:
      query: page.feedlaboratoryimage.toFile
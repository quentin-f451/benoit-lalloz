#-----------------------------------------------------------------------------------

# 1. PROJETS
	
#----------------------------------------------------------------------------------

fields:
  feeds:
    type: pages
    query: site.children.template('projet').filterBy("feedhome", true)
    layout: cards
    text: '{{ page.legendeAuto.toCaption }}'
    image:
      back: white
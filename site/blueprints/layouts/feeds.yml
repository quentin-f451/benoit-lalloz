#-----------------------------------------------------------------------------------

# 1. PROJETS
	
#----------------------------------------------------------------------------------

fields:
  feeds:
    type: quickpages
    query: site.children.template('projet').filterBy("feedhome", true)
    layout: cards
    default: 
      - lalloz
    size: small
    image:
      back: white